/// <reference path="../Refs.ts" />

import Modifier = require("./../Modifier");
import IEffect = require("./../IEffect");
import ObservableCollection = Fayde.Collections.ObservableCollection;

class Effect extends Modifier implements IEffect {

   params = {

   }
}

export = Effect;